import { Layout } from '../ui/templates'

import styles from '../styles/main.module.scss'

const Home = () => {
  return (
    <Layout>
      <h1 className={styles.title}>Filter assignment</h1>
    </Layout>
  )
}

export default Home;
